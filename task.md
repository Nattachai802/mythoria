# งานที่เหลือ

อัปเดต 2026-08-07 — เนื้อหาเดิม (แผน deploy Python service / vector store) ถูก
เคลียร์ออกตามคำสั่ง ยังอ่านย้อนได้จาก git history: `git show 0b08477:task.md`

---

## 1. กฎของระบบพลัง (power rules)

ลงไปแล้ว: ตาราง `power_rules`, CRUD ใน `server/power-rule.ts`, พาเนลในหน้า
`/powers`, ปุ่มดึง `powers.limitations` เดิมมาเป็นกฎ

ออกแบบไว้ว่า **ตารางเดียวคุมทั้งกฎระดับเล่มและกฎรายพลัง** — ต่างกันแค่
`powerIds` ว่าง = ใช้ทั้งเรื่อง, มีรายการ = เฉพาะพลังนั้น อย่าเผลอแตกเป็นสองตาราง

### 1a. ส่งกฎเข้า AI context — ยังไม่ทำ

แทรกกฎที่ scope ตรงกับพลังที่ถูกอ้างถึงเข้าไปใน prompt ตอนให้ AI ช่วยเขียน
เพื่อไม่ให้ AI เขียนหลุดกติกาโลก

ต้องหาให้เจอก่อนว่า prompt ประกอบตรงไหนบ้าง (`server/` มีหลายจุดที่เรียก
Gemini/Groq) แล้วค่อยตัดสินว่าแทรกที่ชั้นไหนให้ไม่ต้องแก้ทุก call site

ระวัง: กฎเยอะ = token บาน ควรส่งเฉพาะกฎที่เกี่ยวกับพลังในฉากนั้น ไม่ใช่ทั้งหมด

### 1b. เตือนเมื่อขัดกฎ — ยังไม่ทำ ก้อนใหญ่สุด

ยังไม่ได้ตัดสินสองเรื่อง ต้องเคาะก่อนเขียนโค้ด:

1. **ตรวจตอนไหน** — ตอนเซฟตอน / ผู้ใช้กดตรวจเอง / ตอน AI เขียนจบ
2. **ตรวจด้วยอะไร** — จับคีย์เวิร์ดตรง ๆ (เร็ว ฟรี พลาดเยอะ) หรือให้ LLM
   อ่านเทียบกฎ (แม่นกว่า เสียเงินทุกครั้ง)

`severity` ในตารางมีไว้รองรับอันนี้อยู่แล้ว — `hard` = ขัดแล้วเตือนแรง,
`soft` = ขัดได้ถ้าเรื่องรับผิดชอบราคาของมัน

---

## 2. สีโน้ตในการ์ดไอเดีย — รอคำตอบ

`CARD_COLORS` ใน `components/plot/playground/canvas-item.tsx:16` มี 10 สี
ที่ `title` โชว์เป็น hex (`#f59e0b`) ผู้ใช้อ่านแล้วไม่ได้อะไร สีไม่มีความหมาย
ไม่มีระบบ — แค่ให้เลือกฟรี ๆ ทุกครั้งที่เขียนโน้ต

สองทาง เลือกอันเดียว:
- ตัดเหลือ 4–5 สีแล้วตั้งชื่อความหมาย (ต้องแก้ / ไอเดีย / ถาม / สำคัญ)
- ตัดทั้งแถวทิ้ง เหลือแต่ "ทั่วไป"

---

## 3. @mention ในโน้ตยังไม่ผูก id

`insertQm` (`canvas-item.tsx:457`) แปะแค่ *ชื่อ* เป็น text เปล่า ผลคือ:
- เปลี่ยนชื่อตัวละคร → โน้ตค้างชื่อเก่า
- หา "โน้ตไหนพูดถึงคนนี้บ้าง" ไม่ได้
- `parseMentions` ใน `lib/mentions.ts` ใช้กับโน้ตพวกนี้ไม่ได้ (มันอ่าน span ของ Quill)

แก้ได้โดยไม่ต้องใช้ Quill: ให้ `insertQm` แปะเป็น `@[ชื่อ](character:id)`
แล้ว render เป็นชิปตอนแสดง ยังเป็น plain text เหมือนเดิม ไม่ต้อง migrate
ไม่ต้อง sanitize

**อย่าเอา Quill มาใส่สติกกี้โน้ต** — เคยพิจารณาแล้ว มันแลก: `notes` เป็น
plain text ต้อง migrate + sanitize (ตอนนี้ playground ไม่มี
`dangerouslySetInnerHTML` เลยสักที่), toolbar สูงกว่าตัวโน้ต, contenteditable
ชนกับ dnd-kit, และ mention popover ที่แบ่งกลุ่ม (คำบรรยาย / ในการ์ดนี้ /
ตัวละครอื่น / ตัวประกอบ) ต้องเขียนใหม่เป็น quill-mention `source`
Quill เหมาะกับที่มันอยู่ตอนนี้ — เขียนตอนกับ lore ยาว ๆ

---

## 4. `lib/story-format.ts` — ที่ค้างจากการรีวิว

รีวิวไฟล์เต็มเมื่อ 2026-08-22 หลังแยก logic ออกจาก `playground-board.tsx`

**แก้ครบแล้วทั้งหมด (2026-08-23)** — รอบแรก: ตัวย่อ `@ชื่อ` ยึด `@` นำหน้า,
`codesByBeat` แทน filter ซ้อน, sticky note ไม่นับเป็นเหตุการณ์ (`isBoardNote`)
รอบสอง: `formatVersion` ลง frontmatter, ตารางสรุปปมกรองเฉพาะปมที่แตะฉาก
(`roles` เก็บทุกตัวไว้ ว่าง = ไม่แตะฉากนี้ ส่วน `dangling` ยังมองทั้งเรื่อง),
cast คีย์ด้วย ชนิด+ชื่อ ไม่ยุบชื่อพ้องคนละชนิด, `itemById` Map แทน
`items.find()` ใน loop, การ์ดคำบรรยายที่มี children บอกตามจริงแทน "ไม่มีตัวละคร",
`causeKind` ไม่รู้จักแสดง raw, escape `|` ในตาราง, `beatCount`/`cardCount`
ไม่นับ sticky note — มีเทสต์คุมทุกจุดใน `lib/story-format.test.ts`

### ยังไม่ทำตามแผน (ไม่ใช่ข้อบกพร่อง)

ข้อ 3 และ 4 ของ "ลำดับการทำ" ใน plan — ขยายจากรายฉากเป็นทั้งเรื่อง และให้
ตัววิเคราะห์พล็อต Phase 1 อ่านจาก format แทนที่จะมี export เป็นผู้ใช้รายเดียว

---

## 5. `zoom: BOARD_ZOOM` ในกระดานพล็อต

`playground-board.tsx:78` ใช้ CSS `zoom` ซึ่งไม่ standard พังกับ pointer
coordinate ของ dnd-kit และเพิ่งมาถึง Firefox โค้ดต้องหารกลับด้วย `BOARD_ZOOM`
ทุกที่ที่คำนวณเส้นเชื่อม (`playground-board.tsx:932-937`)

ถ้าจะ zoom จริงควรเป็น `transform: scale()` แต่ค่าคงที่แบบนี้ลด px แทนก็จบ
ทำตอนเจอบั๊กจริง ไม่ต้องรื้อล่วงหน้า

---

## 6. Mobile responsive

ทั้งแอปเป็น desktop-first ยังไม่เริ่มทำ — งานใหญ่แยกต่างหาก

---

## 7. Plot UI — ค้างจาก impeccable critique (2026-08-27)

รีวิว `/plot` (บอร์ด+วิเคราะห์) กับ `/plot/[eventId]` (playground) เพื่อเตรียมแก้
ยังไม่ได้ลงมือ — คะแนนรวม Nielsen heuristics 27/40 ("พอใช้ได้ ต้องปรับสำคัญ")
จุดอ่อนสุด: ความสม่ำเสมอ 2/4, ความเรียบง่ายเชิงสายตา 2/4, help/onboarding 2/4

### 7a. `resource-sidebar.tsx` อังกฤษล้วน + สีฮาร์ดโค้ด (P0)

ทุกการ์ดบนแคนวาสเริ่มจากลากออกจากไซด์บาร์นี้ แต่ตัวมันเอง:
- ข้อความอังกฤษหมด ("Characters", "Search...", "No characters found",
  "Protagonist/Antagonist/Supporting/Minor") ขัดหลัก Thai-first ของแอป
- สีบทบาทเป็น Tailwind ฮาร์ดโค้ด (`amber-100`, `red-100`...) ไม่ใช้ token
  `--forge-*` เหมือนที่อื่น — implement เป็น `border-l-{color}` ต่อรายการ
  (บรรทัด 228, 234, 240, 247, 255, 262, 269, 297) เลยโดน impeccable detector
  ตีเป็น "side-tab accent border" ซึ่งเป็นแพทเทิร์นที่คนอ่านออกว่าไม่ผ่านการ
  ออกแบบจริง

แก้: แปลข้อความทั้งหมด, ย้ายสีบทบาทไปใช้ `ROLE_META` เดียวกับ `canvas-item.tsx`,
เปลี่ยนจาก border ซ้ายเป็น fill/dot หรือสี icon แทน

**ทำแล้ว (2026-08-27)** — แปล UI ครบ (แท็บ/ค้นหา/empty state/ป้ายบทบาท),
เปลี่ยน `border-l-2` เป็นจุดสี + ป้ายข้อความบทบาทแทนอีโมจิ (กันสื่อความหมายด้วย
สีอย่างเดียว) `ROLE_META` ใน `canvas-item.tsx` เป็นคนละโดเมน (บทบาทในฉาก
ตัวหลัก/ฝ่ายตรงข้าม/ผู้เห็นเหตุ/เหยื่อ ไม่ใช่บทบาทตัวละครตัวเอก/ร้าย/สมทบ/ประกอบ)
เลยไม่เอามาใช้ซ้ำตามที่เขียนไว้ข้างบน — ทำจุดสี inline ในไฟล์นี้แทน

### 7b. ปุ่มลบใช้ `window.confirm()` ดิบ (P0)

`scene-navigator.tsx` (ลบฉาก) กับ `canvas-item.tsx` (`closeQuickNote` ทิ้งโน้ต)
ใช้ dialog เบราว์เซอร์ดิบ ไม่มีธีมมืด ไม่ใช่ Dialog component ที่แอปมีอยู่แล้ว
และเกิดตรงจุดเสี่ยงสุด (ลบฉาก = กู้คืนไม่เต็มที่ตามคอมเมนต์ในโค้ดเอง)

แก้: เปลี่ยนเป็น Dialog/AlertDialog เดิมที่ใช้กับ link-edit / thread-bind dialog

**ทำแล้ว (2026-08-27)** — ลบฉาก: เปลี่ยนเป็น `AlertDialog` ควบคุมด้วย state
(pattern เดียวกับ `delete-chapter-dialog.tsx`) พร้อมบอกว่ากู้คืนได้จากไหน
ทิ้งโน้ต: ไม่ใช้ dialog แยก — เปลี่ยนปุ่ม "ยกเลิก" เป็น pattern สองจังหวะเดียวกับ
ปุ่มลบโน้ต (`confirmDeleteNote`) แทน คือกดครั้งแรกเป็น "ทิ้งข้อความ?" ก่อนยืนยัน
ระหว่างแก้เจอบั๊กเดิมที่ไม่เกี่ยวกัน: `DraggableCanvasItem` ส่ง `tonePresets` เข้า
`CanvasItem` แต่ไม่ได้ destructure รับไว้ (ReferenceError ตอนมี tone preset จริง)
— เพิ่ม `tonePresets` เข้า parameter list ให้ด้วยเพราะเป็นไฟล์เดียวกับที่แก้อยู่แล้ว

### 7c. การ์ดในแคนวาสแน่นเกินหลักการแอปเอง (P1)

การ์ดไอเดีย 1 ใบโชว์พร้อมกันได้ถึง 7 affordance (connect handle, เมนู 3 จุด,
toggle narration, ป้าย key moment, badge ปม, โน้ตสี, เส้น ancestor) แล้วยังบีบ
ด้วย `BOARD_ZOOM=0.8` (ดูข้อ 5 — ของเดิมที่รู้อยู่แล้วว่าเป็น workaround) ขัดหลัก
"แคนวาสควรหลบ ไม่ใช่ประดับ" ที่ตั้งไว้เอง

แก้: ย้าย toggle ที่ไม่บ่อยใช้ (narration, ancestor, สี) เข้าเมนู 3 จุดก่อน
ค่อยแก้ zoom แยกทีหลัง (อ้างข้อ 5)

**ทำแล้ว (2026-08-27)** — ตรวจแล้วพบว่า "เชื่อมเหตุผล" (ancestor) กับ tone/สี
อยู่ในเมนู 3 จุดอยู่แล้วจริง ๆ (`canvas-item.tsx:944-1008`) มีแค่ toggle
"คำบรรยาย" (narration) ที่ลอยเป็นไอคอนแยกตลอดเวลา — ย้ายเข้าเมนูเป็น
DropdownMenuItem แล้ว เหลือไว้แค่ไอคอนเงียบ ๆ (ไม่ใช่ปุ่ม) ตอนเปิดอยู่ เพื่อไม่ให้
เสียการมองเห็นสถานะไปเลย ยังไม่แตะ `BOARD_ZOOM` ตามที่ข้อ 5 บอกไว้

### 7d. `ThreadSuggestToast` โผล่ทุกครั้งที่วางไอเดียใหม่ (P1)

บังคับตัดสินใจเรื่อง "ปม" ทันทีหลังวางการ์ดใบแรก ก่อนผู้ใช้ใหม่รู้จักปมด้วยซ้ำ
ควรเป็น affordance เงียบ ๆ บนการ์ดแทน หรือรอสัญญาณว่าควรผูกปมจริง

**ทำแล้ว (2026-08-27)** — ไม่ลบ toast ทิ้งทั้งหมด (ยังมีประโยชน์กับผู้เขียนที่ใช้
ปมเป็นแล้ว) แต่กันไม่ให้ยิงจนกว่านิยายจะมีปมอยู่แล้วอย่างน้อย 1 เส้น
(`threadState.length > 0`) — นิยาย/ฉากที่ยังไม่มีปมเลยจะไม่โดน popup ตอนวาง
การ์ดใบแรก ผูกปมยังทำได้เสมอผ่านเมนู 3 จุด "ผูกปมเรื่อง" ของการ์ด ระหว่างทาง
แก้บั๊กเล็ก ๆ ที่เจอ: ตัว toast เดิมส่ง `threads` (prop เดิมตอนโหลดหน้า) แทนที่จะ
เป็น `threadState` (state สดที่รวมปมที่เพิ่งสร้างในเซสชันนี้ด้วย) — เปลี่ยนให้ตรงแล้ว

### 7e. เก็บกวาดเล็ก ๆ (P2)

- `StickyNoteItem` สีม่วง/แดง + อังกฤษล้วน ไม่เข้าพวกกับระบบโน้ตสีอำพัน/chamfered
  ที่เหลือใน `canvas-item.tsx` — ตัดสินใจเลิกใช้หรือแก้ให้เข้าธีม
- รหัสการ์ด (`C04`) ใน `finding-row.tsx` กับ `echo-score-panel.tsx` เป็น text
  เฉย ๆ ทั้งที่ `docs/plot-analysis-plan.md` บอกว่าต้องกดแล้วเลื่อนไปหาการ์ดด้วย
  `handleCenterOnItem` — ยังไม่ได้ต่อสาย
- อังกฤษหลุดที่อื่น: เมนู export ("JSON (backup / re-import)"),
  `SceneNavigator` ("Search scene...", "No scene found.", "New Scene")
- `group-frame.tsx` (325 บรรทัด) น่าจะเป็นโค้ดตาย — `playground-board.tsx`
  filter `type==='group'` ทิ้งพร้อมคอมเมนต์บอกว่าเลิกใช้แล้ว
- `ThreadSuggestToast` เลือกปมแรกในลิสต์ไว้ล่วงหน้า — กด "ผูกปม" เร็วไปอาจผูกผิดปม

### อย่าไปแตะ — ทำงานดีอยู่แล้ว

เส้นเชื่อมตั้งฉากแบบ fan-out/converge ใน `playground-board.tsx`, แท็บ "วิเคราะห์"
(โชว์หลักฐานไม่ให้คะแนน ปุ่มเดียว "ไม่ใช่ปัญหา" — ตามแผนที่ตั้งใจ), autosave + undo

### ยังไม่เคาะ ต้องตอบก่อนลงมือ

1. แคนวาสฉากมีระบบซ้อนกันเยอะ (เลน/จังหวะ/ตอน/เส้น/ปม/key moment/narration/
   โน้ต/echo score) — แยกเป็นโหมด (โครงสร้าง vs รายละเอียดจุด) ไหม หรือซ่อนของ
   ที่ไม่บ่อยใช้เข้าเมนูแทนแล้วปล่อยให้เห็นหมดเหมือนเดิม
2. `BOARD_ZOOM=0.8` เคยเอาไปให้คนเขียนจริงลองยาว ๆ หรือยัง (ข้อ 5 เดิมก็ค้าง
   คำตอบนี้อยู่)

---

## 8. Echo Score — ทดลอง format ประหยัด token + โมเดลถูกกว่า (2026-09-02)

สองวัตถุประสงค์คู่กัน: (1) หา format ข้อความไทยที่ป้อนให้โมเดลแล้วกิน token
น้อยที่สุด (2) หาโมเดลถูกกว่า `gemini-2.5-flash` ที่ยัง "เข้าใจแก่นเรื่องจริง ๆ"
ไม่ใช่แค่ parse JSON ผ่าน

**เขียนสคริปต์แล้ว ยังไม่ได้รันจริง** (มีค่าใช้จ่ายจริงทุกครั้งที่รัน):

- `scripts/compare-echo-models.ts` (objective 2) — แก้จากของเดิม: รวมทุก
  candidate ยิงผ่าน OpenRouter เส้นทางเดียว (ตัด direct-Gemini SDK ทิ้ง — เครื่อง
  dev นี้ไม่มี `GEMINI_API_KEY`/`TYPHOON_API_KEY` ใน `.env` เลย ตาม fallback chain
  จริงของ `echo-score` ใน `lib/ai-features.ts:103` ก็ตกไปที่ deepseek ผ่าน
  OpenRouter อยู่แล้วตอนนี้ ไม่ใช่ gemini ตามที่ค่าคงที่บอก) ใส่ Gemini เข้าไปเป็น
  candidate ผ่าน OpenRouter (`google/gemini-2.5-flash`) แทน เพิ่ม **referee
  model** (`anthropic/claude-sonnet-4.5` — เช็ค slug ยังใช้ได้ก่อนรัน) ตัดสินซ้ำ
  แบบไม่รู้ว่าเดามาจากโมเดลไหน แยกจาก self-judge เดิม กัน bias ที่โมเดลตัดสิน
  เข้าข้างตัวเอง — ตารางสรุปโชว์ self hits/K คู่กับ referee hits/K พร้อม flag
  `⚠ self>>referee`

- `scripts/compare-thai-formats.ts` (objective 1, ไฟล์ใหม่) — เนื้อหาความหมาย
  เดียวกัน (ชุด "prefix ยาว" เดียวกับสคริปต์แรก) เทียบ 5 format: ปัจจุบัน
  (bracket+dash) / ตัดวงเล็บ-ขีด / ตัดวรรคในคำไทย / คั่นด้วย `|` / key ย่อแบบ
  YAML วัด `prompt_tokens` จริงจาก API (`max_tokens=1` ประหยัดต้นทุนทดลอง)
  ไม่เดาด้วย tokenizer ท้องถิ่นเพราะแต่ละ provider tokenize ไทยต่างกันมาก

**ต้องรอทำต่อ:**
1. รันทั้งสองสคริปต์จริง (`npx tsx scripts/compare-echo-models.ts` /
   `npx tsx scripts/compare-thai-formats.ts`) — ยังไม่ได้กดเพราะเสียเงินจริง
2. ถ้า format ไหนชนะเรื่อง token ต้องเอาไปวัดคุณภาพซ้ำผ่าน
   compare-echo-models.ts ก่อน ไม่ตัดสินจากตัวเลข token อย่างเดียว (เขียนเตือนไว้
   ในคอมเมนต์หัวไฟล์แล้ว)
3. ถ้าเลือกโมเดล/format ใหม่ได้ ต้องแก้จริงที่ `lib/echo-score.ts`
   (`buildPrefixText`/`buildCardText`) และ `lib/ai-features.ts` (`chain` ของ
   `echo-score`) — ยังไม่ได้แตะโค้ด production เลย

---

## นอกขอบเขต

- **อย่ารัน `drizzle-kit generate` + `db:migrate`** — `migrations/` ค้างอยู่นาน
  ที่ผ่านมาใช้ `db:push` ตลอด สั่ง generate ทีเดียวมันจะเก็บ drift สะสมมาทั้งหมด
  (`CREATE TABLE world_systems`, `faction_relationships`, `invitations` ที่มีใน
  DB จริงแล้ว) รันแล้วพังทันที ใช้ `npm run db:push` อย่างเดียว
- Context Fabric normalization — พักไว้ ไม่ต้องรื้อจนกว่าจะสั่ง
