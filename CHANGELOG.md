# Changelog

> ไฟล์นี้ generate จาก `lib/changelog.ts` ด้วย `npm run changelog` — **ห้ามแก้มือ**

## [2.5.0] — 2026-09-05

**เช็คลิสต์บทนี้มีอะไรต้องแก้**

### เพิ่มใหม่

- ลบไอเดียทิ้งได้จากแถบทรัพยากรในกระดานพล็อต ไม่ต้องย้อนกลับไปหน้าคลังไอเดีย
  - ชี้ที่การ์ดไอเดียแล้วกดถังขยะ · มีกล่องยืนยันบอกชัดว่าลบออกจากคลังทั้งโปรเจกต์และกู้คืนไม่ได้
- ลากการ์ดบนกระดานพล็อตด้วยคีย์บอร์ดได้แล้ว (เว้นวรรคเพื่อจับ ลูกศรเพื่อเลื่อน)
- ปุ่ม "เช็คลิสต์บทนี้" บนหน้าภาพรวมบท — รวมจุดที่ยังไม่เสร็จไว้ที่เดียว
  - ปมค้างที่แตะบทนี้ · ฉากไม่มี causal link · ยังไม่ตั้ง POV · ยังไม่ตั้งจังหวะ · เหตุ-ผลที่ตรวจแล้วว่าไม่สมเหตุผล · จังหวะพล็อตที่คาดเดาง่าย — อ่านจากข้อมูลที่มีอยู่แล้วทั้งหมด ไม่เรียก AI เพิ่ม
- เพิ่ม “พลัง” เข้ากระดานพล็อตได้แล้ว — ลากจากแถบซ้าย หรือใส่เป็นผู้ร่วมฉากในการ์ด
  - กรอกได้ว่าพลังนั้นถูกใช้ยังไงในฉาก · มีชิปพลังที่ใช้บ่อยให้กดเร็ว · AI ทุกตัวเห็นข้อมูลพลังในฉากทันทีโดยไม่ต้องตั้งค่าเพิ่ม
- กระดานพล็อตรับผู้ร่วมฉากได้ 6 ชนิดแล้ว — ตัวละคร / กลุ่มฝ่าย / สัตว์-ภูต / พลัง / สิ่งของ / ระดับในระบบโลก
  - ตอบได้ว่าของชิ้นไหนโผล่ฉากไหน มอนสเตอร์ตัวนี้สู้ที่ไหนบ้าง พลังนี้ถูกใช้ตอนไหน · ระบบโลกผูกเป็น “ระดับ” (เช่น ระบบยศ › A) ไม่ใช่ทั้งระบบลอย ๆ · ทุกชนิดกรอกได้ว่าเกี่ยวข้องยังไงในฉาก และ AI เห็นข้อมูลนี้ทันที
- ผู้ช่วยดูจังหวะในฉาก — บอกว่าตอนนี้เอื่อย เร่งค้าง หรือแบน แล้วเสนอว่าจังหวะถัดไปควรเป็นแบบไหน
  - อ่านจากจังหวะที่ตั้งไว้เอง ไม่เรียก AI · ถ้ากรอกไว้ไม่ถึง 60% จะขึ้นปุ่มให้ AI ช่วยอ่านแทน โดยไม่เขียนทับค่าที่ตั้งเองและไม่บันทึกลงการ์ด
- หน้า AI Control เก็บคำตอบดิบของ AI ไว้ตอนที่ระบบอ่านผลลัพธ์ไม่ออก (เรื่องภายใน)
  - เก็บเฉพาะตอนพังเท่านั้น ตอนสำเร็จไม่เก็บ เพื่อไม่ให้เนื้อเรื่องของผู้ใช้ไหลเข้า log · สถานะใหม่ parse_error แยกจาก error (ยิงสำเร็จแล้วแต่รูปแบบผลลัพธ์อ่านไม่ได้)

### ปรับปรุง

- ยกเครื่องแถบทรัพยากรซ้ายของกระดานพล็อต — รู้ทันทีว่าปุ่มไหนคืออะไร
  - ทุกแท็บมีชื่อกำกับใต้ไอคอน ไม่ต้องเดาหรือชี้ค้างรอ tooltip · ปุ่มใหญ่พอสำหรับนิ้ว · หน้าตาเข้าชุดกับกระดาน (มุมตัด + สีอำพัน) · แท็บที่ยังไม่มีข้อมูลบอกสาเหตุแล้วว่า “ยังไม่มี” หรือ “ค้นหาไม่เจอ” จากเดิมที่บางแท็บว่างเปล่าเฉย ๆ · สีทุกจุดใช้ได้จริงในธีมมืด
- รายการทรัพยากรที่ยาวมากแบ่งเป็นชุดละ 30 มีปุ่ม “แสดงเพิ่ม”
  - โปรเจกต์ที่มีฝ่ายหรือตัวละครเป็นร้อย เปิดแท็บแล้วไม่หน่วง และบอกด้วยว่ากำลังแสดงกี่จากทั้งหมดเท่าไร
- ลากทรัพยากรลงกระดานแล้วเห็นชิ้นเดิมติดมือ แทนที่จะกลายเป็นการ์ดใบใหญ่ทันทีที่จับ
- Tooltip เส้นจังหวะ AI เปลี่ยนคำจาก "AI แนะนำ" เป็น "AI อ่านว่า" — กันเข้าใจผิดว่าเป็นคำสั่งให้แก้

### แก้บั๊ก

- แก้เว้นวรรคไม่ได้ตอนพิมพ์โน้ตในการ์ดบนกระดานพล็อต
  - ระบบลากด้วยคีย์บอร์ดที่เพิ่งเพิ่มเข้ามาดักปุ่มเว้นวรรคไปใช้เริ่มลากการ์ด แม้ตอนนั้นกำลังพิมพ์อยู่ในช่องข้อความ
- แก้ฟีเจอร์ AI ที่ยิงพลาดทุกครั้งก่อนจะไปสำเร็จที่ผู้ให้บริการสำรอง — เร็วขึ้นและเสถียรขึ้น
  - โมเดลเดิมของ groq ถูกย้ายไปเป็นแพ็กเกจ enterprise ทำให้คืน 404 ทุกครั้ง · เปลี่ยนเป็น qwen3.8-27b ที่ทดสอบยิงจริงแล้วว่าใช้ได้และตอบตรงรูปแบบที่ระบบต้องการ
- แก้ AI ช่วยคิดจังหวะที่ขึ้น "แนะนำไม่สำเร็จ" ทั้งที่ AI ตอบมาครบแล้ว
  - โมเดลตอบคนละรูปแบบกับที่ระบบรองรับ ทำให้ทิ้งผลทั้งชุดและเสีย token ฟรี — ตอนนี้อ่านได้ทั้งสองรูป

## [2.4.0] — 2026-09-04

**จังหวะการเล่า + ประตูบริบทพล็อต**

### เพิ่มใหม่

- ตั้ง “จังหวะการเล่า” ได้ทั้งระดับฉากและระดับการ์ดไอเดีย
  - สเกล 1-10 อยู่ในแผงโครงฉากดราม่า คนละมิติกับผลลัพธ์ฉาก (สำเร็จ/ล้มเหลว)
- เส้นจังหวะรายฉากบนหน้าภาพรวมบท
  - ดูทีละฉาก เลื่อนด้วยปุ่มหรือปุ่มลูกศร เทียบกับโครงเรื่องมาตรฐานหรือรูปแบบรายบทได้
- AI ช่วยคิดจังหวะ — ให้คะแนนเองแล้ววาดเป็นเส้นปะเทียบกับที่เราตั้งไว้
  - บอกเหตุผลและความมั่นใจของแต่ละจุด · AI จะไม่เห็นค่าที่เราตั้งไว้ก่อน เพื่อไม่ให้ตอบตามกัน
- ชิปตัวละครที่ใช้บ่อยในแผงผู้เข้าร่วม กดครั้งเดียวเพิ่มเข้าไอเดียเลย
  - เรียงตัวที่อยู่ใน 2 ฉากก่อนหน้าไว้หน้าสุด รองรับทั้งตัวละครจริงและชื่อชั่วคราว
- Unified Scene Framework — จัดประเภทฉากเป็น 5 แบบ (ปูพื้น/รุกฆาต/รับแรง/แตกหัก/คลี่คลาย)
  - ชื่อช่องกรอกเปลี่ยนตามประเภทที่เลือก มีปุ่มให้ AI ช่วยเดาประเภทจากโครงที่วางไว้
- หน้า “มีอะไรใหม่” — อ่านบันทึกการเปลี่ยนแปลงทุกรุ่นได้ในแอป
  - มีจุดแจ้งเตือนบนเมนูเมื่อมีรุ่นใหม่ที่ยังไม่ได้อ่าน · จำเฉพาะในเบราว์เซอร์เครื่องนั้น
- ระบบบันทึกอัปเดตแบบแหล่งข้อมูลเดียว (เรื่องภายใน)
  - เขียนที่ lib/changelog.ts ที่เดียว แล้วป้อนทั้ง CHANGELOG.md หน้าในแอป และเลข version · npm run check บังคับให้ package.json กับ README ตรงกันเสมอ
- Plot Context — ประตูกลางสำหรับป้อนบริบทกระดานพล็อตให้ AI (เรื่องภายใน)
  - ฟีเจอร์ AI ประกาศว่าต้องการบริบทระดับไหน แทนที่จะไปประกอบข้อมูลเองทีละตัว

### ปรับปรุง

- เส้นจังหวะไม่แสดงชื่อฉาก/การ์ดค้างไว้แล้ว — ชี้ที่จุดเพื่อดูแทน

## [2.3.1] — 2026-08-31

**Echo Score v2 + สรุปพล็อต**

### เพิ่มใหม่

- Plot Recap + ตรวจความสมเหตุผล — สรุปฉาก/บท และเช็คเหตุ-ผลในคอลเดียว

### ปรับปรุง

- Echo Score v2 — สลับผู้ให้บริการอัตโนมัติเมื่อเจ้าแรกล่ม บังคับรูปแบบผลลัพธ์ที่ระดับ API
  - ใช้ตัวย่อชื่อตัวละครเพื่อลด token · เพิ่มตัวตรวจจุดพลิกผังจากข้อมูลเดิมโดยไม่มีค่าใช้จ่าย
- การ์ดไอเดียเปลี่ยนจากหน้าต่างเต็มเป็น popover เทียบได้หลายใบพร้อมกัน

### แก้บั๊ก

- แก้บั๊ก Gemini ที่กินโควตาฟรีของทุกฟีเจอร์
