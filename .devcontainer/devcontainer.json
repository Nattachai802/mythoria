{
  "name": "Mythoria Dev",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "bradlc.vscode-tailwindcss",
        "ms-python.python",
        "mtxr.sqltools"
      ]
    }
  },
  "forwardPorts": [3000, 8000],
  
  // คำสั่งที่ให้รันอัตโนมัติเมื่อ Container ถูกสร้างเสร็จ (ทำงานเรียงลำดับ)
  // 1. ลงไลบรารี Node
  // 2. จำลองสภาพแวดล้อม Python และติดตั้งแพ็กเกจ
  // 3. ดึงข้อมูลตัวอย่างจากไฟล์ backup.sql เข้า Database (ถ้ามีไฟล์)
  // 4. อัปเดต Schema ล่าสุดผ่าน Drizzle
  "postCreateCommand": "npm install && cd pythonservice && python3 -m venv venv && ./venv/bin/pip install -r requirements.txt || true && cd .. && if [ -f backup.sql ]; then psql \"postgresql://postgres:1234@localhost:5432/mythoria_db\" -f backup.sql || true; fi && npm run db:push",
  
  "remoteUser": "node"
}
