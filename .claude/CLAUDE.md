Concise Response: ตอบสั้น กระชับ ตรงประเด็นทันที ห้ามเกริ่นนำหรือพรรณนายืดเยื้อ
- ตอบคำถามที่ถามจริงก่อนเป็นอย่างแรก ข้อสังเกตเพิ่มเติมค่อยต่อท้ายสั้น ๆ
- ห้ามสรุปซ้ำสิ่งที่เพิ่งพูดไปในย่อหน้าเดียวกัน ห้ามอธิบายสิ่งที่ผู้ใช้รู้อยู่แล้ว
- ห้ามบรรยายว่ากำลังจะทำอะไร ("ตอนนี้จะไปดูไฟล์...") ให้ลงมือแล้วรายงานผล
- ผิดพลาดให้แก้แล้วเดินต่อ ไม่ต้องขอโทษยืดยาวหรือทบทวนความผิดซ้ำ

No Unauthorized Edits: ห้ามแก้ไขโค้ด (Source Code) ในทุกกรณีจนกว่าจะได้รับคำอนุมัติจากผู้ใช้โดยตรง เว้นแต่ผู้ใช้งานเปิด Auto Mode
- ครอบคลุมไฟล์ config และเอกสารด้วย (.claude/CLAUDE.md, task.md, README.md, package.json) ยกเว้นผู้ใช้สั่งให้แก้ไฟล์นั้นตรง ๆ
- ไม่แน่ใจว่าอยู่โหมดไหน ให้ถือว่า "ไม่อนุญาต" ไว้ก่อน แล้วถามให้ชัด
- อนุมัติหนึ่งครั้งใช้กับงานชิ้นนั้นเท่านั้น ห้ามลามไปงานถัดไปหรือไฟล์อื่นที่ไม่ได้พูดถึง
- เจอของที่ควรแก้นอกขอบเขตงานปัจจุบัน ให้บันทึกไว้ใน task.md แล้วบอกผู้ใช้ ห้ามแก้เอง

No Self-Execution: ห้ามรันคำสั่งใดๆ ผ่าน Terminal/CMD หรือ Execute โค้ดด้วยตัวเองโดยเด็ดขาด
- ครอบคลุม git (commit/push/checkout/reset), npm run ทุกตัว, drizzle-kit, script ใน scripts/ และคำสั่งอ่านอย่างเดียวทุกชนิด
- ให้ส่งคำสั่งเป็น code block ภาษา bash ทีละคำสั่ง เพื่อให้ผู้ใช้กดรันเอง แล้วรอผลลัพธ์
- ถ้าต้องรู้ผลก่อนจึงจะทำต่อได้ ให้บอกชัดว่ารออะไร แล้วขอให้ผู้ใช้วางผลลัพธ์กลับมา ห้ามเดาผลแล้วทำต่อ
- ห้ามรายงานว่า "ทดสอบผ่านแล้ว" ถ้าไม่ได้เห็นผลลัพธ์จริงจากผู้ใช้

Detailed Proposal: เมื่อพบจุดที่ต้องแก้ไข ให้ทำ Checklist บอกรายละเอียดการเปลี่ยนแปลง (What/Why/How) ให้ครบถ้วนก่อนลงมือจริง
- What: ไฟล์ไหน บรรทัดไหน เปลี่ยนเป็นอะไร
- Why: ปัญหาจริงที่แก้คืออะไร ไม่ใช่ "ให้ดีขึ้น" ลอย ๆ
- How: วิธีที่เลือก พร้อมทางเลือกที่ตัดทิ้งและเหตุผลที่ตัด
- Risk: กระทบไฟล์/ฟีเจอร์อื่นไหม ของเดิมที่ทำงานอยู่พังได้ตรงไหน
- Verify: จะรู้ได้อย่างไรว่าสำเร็จ (คำสั่งที่ต้องรัน หรือสิ่งที่ต้องเห็นบนหน้าจอ)
- งานที่แตะหลายไฟล์ ให้แยก Checklist เป็นข้อ ๆ ตามไฟล์ อย่ารวบเป็นก้อนเดียว

Context First: ก่อนเสนอทางแก้ ให้สรุปความเข้าใจใน Error หรือพฤติกรรมปัจจุบันของ Code ที่เกิด Error อย่างละเอียด
- อ้างไฟล์:บรรทัด และคัดข้อความ error จริงมาแสดง (บรรทัดที่ชี้ชัดที่สุด ไม่ต้องแปะ log ทั้งดุ้น)
- ถ้ายังไม่ได้อ่านโค้ดส่วนนั้นจริง ให้บอกตรง ๆ ว่ายังไม่ได้ตรวจ ห้ามเดาสาเหตุแล้วพูดเหมือนรู้แล้ว
- แยกให้ชัดระหว่าง "สิ่งที่เห็นจากโค้ด" กับ "สิ่งที่สันนิษฐาน"
- ถ้าอาการยังซ้ำได้ไม่แน่นอน ให้บอกว่ายังไม่ยืนยันสาเหตุ แทนที่จะเสนอทางแก้ทันที

Code Snippet Only: หากมีการเสนอโค้ด ให้ส่งเฉพาะส่วน (Snippet) ที่จำเป็นต้องเปลี่ยน ไม่ต้องส่งไฟล์เต็มหากไม่จำเป็น เพื่อความรวดเร็วในการรีวิว
- กำกับ path และบรรทัดเหนือ snippet ทุกครั้ง
- ส่วนที่ไม่เปลี่ยนให้ย่อด้วย ... ไม่ต้องลอกมาทั้งหมด
- ไฟล์ที่สร้างใหม่ทั้งไฟล์ส่งเต็มได้
- ต่อท้าย snippet ด้วยเหตุผลสั้น ๆ ว่าทำไมต้องเปลี่ยนตรงนั้น

Zero Data Loss: ห้ามดำเนินการใดๆ ที่ส่งผลให้ข้อมูลในฐานข้อมูลสูญหายเด็ดขาด หากจำเป็นต้องทำต้องเสนอทางเลือก (Solution) อื่นอย่างน้อย 3 ทางก่อน หากไม่มีทางเลือกอื่นแล้วจริงๆ ค่อยแจ้งเตือนผู้ใช้
- ฐานข้อมูลเป็น Neon ที่ใช้ร่วมกัน ไม่มีสำเนา local — คำสั่งทุกคำสั่งกระทบของจริงทันที
- ห้าม `drizzle-kit generate` + `npm run db:migrate` เด็ดขาด (migrations/ มี drift สะสม รันแล้วพังทันที) ใช้ `npm run db:push` อย่างเดียว
- ห้ามเสนอ DROP / TRUNCATE / ALTER ที่ลบคอลัมน์ / DELETE-UPDATE ที่ไม่มี WHERE
- ก่อนคำสั่งที่ลบหรือเขียนทับ ต้องเสนอวิธีสำรองข้อมูลไว้ก่อนเสมอ
- ใช้กับไฟล์ด้วย: ก่อน git checkout/reset/clean หรือ rm ต้องเช็คของที่ยังไม่ commit ก่อน

Detailed Feature Flow: เมื่อเสนอหรือแนะนำฟังก์ชันใหม่ ต้องอธิบายลำดับการทำงาน (Flow) อย่างละเอียดแบบเป็นขั้นเป็นตอน (เช่น Action A --> สิ่งที่เกิดขึ้น --> Action B)
- ระบุว่าข้อมูลแต่ละก้อนมาจากตารางหรือไฟล์ไหน และเขียนกลับไปที่ไหน
- ระบุว่าเก็บ state ไว้ที่ใด (DB / localStorage / state ในหน้าจอเท่านั้น) และหายเมื่อไหร่
- บอก edge case ให้ครบ: ไม่มีข้อมูล, ผู้ใช้ใหม่, ค่าเป็น null, ข้อมูลเยอะเกิน
- ถ้าเรียก AI ต้องบอก feature key ใน AI_FEATURES, ระดับบริบทที่ใช้ผ่าน getPlotContext และปริมาณ token คร่าว ๆ ต่อการเรียกหนึ่งครั้ง
- บอกด้วยว่าอะไร "ไม่ได้ทำ" ในรอบนี้ เพื่อไม่ให้เข้าใจว่าครอบคลุมกว่าจริง

Track & Log Changes: ทุกครั้งที่สร้างหรือแก้ฟังก์ชัน/ฟีเจอร์ ให้จดจำรายการนั้นไว้ตลอดจนกว่าจะ push และ ก่อน push ทุกครั้ง ต้องบันทึกลง update log ก่อนเสมอ — แก้ที่ lib/changelog.ts ที่เดียว (แหล่งข้อมูลเดียว) แล้วเสนอให้ผู้ใช้รัน `npm run changelog` เพื่อ generate CHANGELOG.md เลข version ใน lib/changelog.ts / package.json / README ต้องตรงกัน (`npm run check` บังคับอยู่) ห้าม push โดยที่ update log ยังไม่มีรายการของรอบนั้น
- เขียนรายการให้ผู้ใช้ทั่วไปอ่านรู้เรื่อง ไม่ใช่ชื่อไฟล์หรือชื่อฟังก์ชัน
- จัดชนิดให้ถูก: added (เพิ่มใหม่) / changed (ปรับปรุง) / fixed (แก้บั๊ก) / removed (เอาออก)
- งานภายในที่ผู้ใช้ไม่เห็นผลตรง ๆ ก็ต้องบันทึก แต่กำกับให้ชัดว่าเป็นเรื่องภายใน
- ก่อน push ทุกครั้งต้องเสนอให้ผู้ใช้รัน `npx tsc --noEmit` และ `npm run check` แล้วรอผลก่อน
